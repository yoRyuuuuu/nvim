return {
  "kylechui/nvim-surround",
  event = "VeryLazy",
  version = "^3.0.0",
  opts = {
    -- Stay put after surrounding so editing can continue (no cursor jump).
    move_cursor = false,
  },
}
